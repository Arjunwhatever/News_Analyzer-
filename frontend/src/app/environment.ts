/**
 * Centralised environment configuration.
 * Change the base URL here and every service picks it up automatically.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7121/api'
};
